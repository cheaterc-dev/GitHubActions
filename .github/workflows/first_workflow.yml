name: First Workflow
on: [push]

jobs: 
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display some Text
        run: echo "Some text"
      - name: Command in Multiline
        run: |
          node -v
          npm -v
